pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'we are executing build'
                echo 'this is main branch'
                
            }
        }
    }
}
